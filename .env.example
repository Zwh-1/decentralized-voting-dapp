# 这是给**容器**用的环境变量，不是给"从仓库直接跑应用"用的。
#
#   cp .env.example .env      # 然后填值
#
# 【生产部署请改用 deploy-env/server.env】那份才是完整模板：除了这里的字段，
# 它还含 mysql 口令、Grafana 管理口令、告警邮件与 WEB_IMAGE 共 20 个字段。
# 这份是仓库内的最小示例，缺 WEB_IMAGE 时生产 compose 会直接报错（刻意的）。
#
# `web/.env` 是另一个文件、干另一件事：它配置的是 `pnpm dev` 和直接从检出目录
# 跑的脚本，那里的数据库是发布到宿主机的 127.0.0.1:3307。而在 compose 网络
# 内部，主机名是 `mysql`、端口是 3306，所以这两个文件**不可能是同一个文件**。
#
# 这里不允许带值提交。`HOSTNAME` 由镜像设置，`PORT` 也是，所以两者都不该出现在
# 这个文件里。

# 只读的链上访问。这个地址不通时，服务是降级而不是崩溃，
# 但 /api/health 会如实说出来。
#
# 生产 compose 把这两个写成 `${RPC_URL:?}` / `${CHAIN_ID:?}`：文件里缺任何一个，
# compose 拒绝渲染，而不是让容器退回 127.0.0.1:8545 与 chain 31337 继续"看起来正常"。
RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
CHAIN_ID=11155111

# 一个区块要落后链头多少块才被索引。用于防重组。
CONFIRMATIONS=5

# 索引时每次 RPC 请求取多少个区块。
CHUNK_BLOCKS=2000

# 索引器 worker 多久开始下一轮拉取。
POLL_INTERVAL_MS=4000

# 只有"应该自己推进索引"的进程才设为 true。
# compose 文件把 web 服务与 indexer 服务**都**设为 false，
# 因为 indexer 自己的循环是由它的 entrypoint 驱动的。
INDEXER_ENABLED=false

# 这里曾经有一个 LOG_LEVEL=info，已删除：没有任何代码读它。
# 详见 deploy-env/server.env 里的同一处说明。

# 在 compose 网络内部，数据库用服务名访问 —— 这就是它与 web/.env 里那份
# DATABASE_URL 的区别。
#
# 用户名/口令必须与 .env 里的 MYSQL_USER / MYSQL_PASSWORD 一致：后两者由
# docker-compose.yml 插值读入，用来初始化 mysql 容器的账号。
DATABASE_URL=mysql://voting:voting@mysql:3306/voting

# 下面三项**不在这个文件里生效**。它们在构建镜像时被内联进 bundle，
# 运行时再设置也不会改变任何东西——真正的填入点是 GitHub Variables
# （见 deploy-env/github-actions.txt）或 `docker build --build-arg`。
# 列在这里只为说明默认值是什么。
NEXT_PUBLIC_LOCAL_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
NEXT_PUBLIC_IPFS_GATEWAY=https://ipfs.io/ipfs/
