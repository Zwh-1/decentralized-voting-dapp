# `contracts` 包的环境变量。
#
# Hardhat 3 通过 hardhat.config.ts 里的 `configVariable(...)` 读取这些变量。
# 你可以直接 export 成环境变量，也可以用 keystore 加密保存（推荐）：
#
#   npx hardhat keystore set SEPOLIA_RPC_URL
#   npx hardhat keystore set SEPOLIA_PRIVATE_KEY
#   npx hardhat keystore set ETHERSCAN_API_KEY
#
# 如果你更想用明文文件，就把这份文件复制成 `.env` 再填。
# hardhat.config.ts 会**显式**加载 contracts/.env，因为 Hardhat 3 自己不会去读 .env
# 文件。shell 里 export 的变量优先级高于文件，所以临时覆盖仍然有效。
# `.env` 已被 git 忽略；这份示例文件不忽略。

# 任意 Sepolia JSON-RPC 端点。公共端点即可，不需要 API key。
# 下面这两个都实测返回过 chain id 11155111：
#   https://ethereum-sepolia-rpc.publicnode.com
#   https://1rpc.io/sepolia
# （rpc.sepolia.org 试过，返回 HTTP 404，因此不列在这里。）
SEPOLIA_RPC_URL=

# 专门用于部署的一次性私钥。**绝不要用持有真实资金的私钥。**
# 部署前先从 faucet 领一点 Sepolia 测试 ETH 打进去。
#
# 只有部署需要它。`verify:sepolia` 跑在无账户的 `sepoliaReadOnly` 网络上，
# 不读这个变量。
SEPOLIA_PRIVATE_KEY=

# 从 https://etherscan.io/myapikey 申请的免费 key —— 只有 `verify:sepolia` 需要。
# 注意 Etherscan V2 API 用同一个 key 服务所有支持的链，所以一个 key 就覆盖
# Sepolia。
ETHERSCAN_API_KEY=

# `VOTING_OWNER` 以前放在这里，用来指定那个唯一合约的管理员。
# 它被删掉了，因为已经没有"唯一管理员"这个东西可指定了：factory 完全没有
# owner（任何人都可以创建投票），而每个投票的 owner 就是创建它的那个地址，
# 由 `createPoll` 记在链上。把这个变量继续文档化，只会诱使运维去设置它、
# 然后以为它起了作用。

# Pin 服务的凭据，只有 `pnpm pin:metadata` 需要。那个脚本把
# contracts/metadata/*.json 原样上传，并记录服务返回的 CID ——
# 但在此之前它会拿这个 CID 与仓库自己为同样字节算出的 CID 比对，
# 所以一个重新序列化了文档、或用 `scripts/cid.ts` 不认识的 profile 导入的
# 服务，会在这一步失败，而不是把一个无法解析的字符串写到链上。
#
# Pinata 签发的两种形式都接受；如果两个都设置了，以 JWT 为准。
# 从 https://app.pinata.cloud/developers/api-keys 获取。
# 两个值都**从不打印**：报告只说变量名，从不说它们的值。
# 没有别的脚本用、也没有任何测试用 —— CI 不做 pin。
# PINATA_JWT=
# PINATA_API_KEY=
# PINATA_SECRET_KEY=
