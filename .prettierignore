node_modules/
dist/
artifacts/
cache/
coverage/
pnpm-lock.yaml

# Solidity is formatted by its own tooling (forge fmt / the Solidity style
# guide). Prettier has no parser for it here, so keep it out of the JS pipeline.
*.sol

# GENERATED by `pnpm export-abi` — formatting them would create a diff that the
# next export undoes, and CI checks these files for drift.
packages/shared/src/voting-abi.ts
packages/shared/src/deployments.ts
